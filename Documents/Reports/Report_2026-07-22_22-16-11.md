# Report — 2026-07-22 22:16:11

## Yêu cầu thay đổi

1. Form chỉnh sửa hồ sơ: xoá nút *Khai báo sau* / *Gửi khai báo*, *Lưu và tiếp tục*, *Làm mới*, *Huỷ* và logic liên quan; thêm nút **Lưu** sau *Xem chi tiết* (phương án A).
2. Lifecycle trạng thái hồ sơ:
   - Chưa khai báo ↔ đủ/thiếu bắt buộc khi lưu
   - Đủ bắt buộc → Chờ xác nhận (auto)
   - HR từ Chờ xác nhận → Đã xác nhận / Cần điều chỉnh / Khóa
   - Gửi yêu cầu chỉnh sửa → Yêu cầu chỉnh sửa; HR duyệt → Cần điều chỉnh; từ chối/huỷ → Đã xác nhận
   - Sau chỉnh sửa + lưu: đủ → Chờ xác nhận; thiếu → Chưa khai báo

## Việc đã thực hiện

### UI (`EmployeeFormPage`)
- Bỏ cụm nút dưới form và `submitDeclaration` / auto-complete khi lưu.
- Thêm **Lưu** cạnh *Xem chi tiết*; lưu `PATCH` rồi cập nhật form/status tại chỗ (không navigate).

### Shared + Prisma
- Thêm `EmployeeProfileStatus.EDIT_REQUESTED` (“Yêu cầu chỉnh sửa”).
- `AUTO_LIFECYCLE_STATUSES`: INCOMPLETE, NEEDS_ADJUSTMENT.
- Cập nhật `HR_EMPLOYEE_STATUS_TRANSITIONS` (PENDING_REVIEW → VERIFIED / NEEDS_ADJUSTMENT / LOCKED).
- Migration `20260722220000_employee_profile_edit_requested`.
- Label/color trên `employee-catalogs.ts`.

### API
- `update`: chặn sửa khi LOCKED / EDIT_REQUESTED; sau lưu gọi `syncLifecycleStatusAfterSave`.
- `complete`: tương thích — sync lifecycle, lỗi nếu chưa đủ để về PENDING_REVIEW.
- `createEditRequest`: chỉ từ VERIFIED → set EDIT_REQUESTED.
- `cancelEditRequest` / reject: trả về VERIFIED; approve → NEEDS_ADJUSTMENT.

### Tests
- `personal.e2e-spec`, `employees.e2e-spec` cập nhật theo lifecycle mới — **passed**.

## Kết quả thực hiện

- **Thành công:** UI gọn (một nút Lưu); trạng thái tự động theo đủ/thiếu bắt buộc; luồng yêu cầu chỉnh sửa có status riêng.
- Dev: migrate + build + restart API `:3001`.
- Hard-refresh form edit để nhận UI mới.
