# Report: Đồng bộ hiển thị công ty chủ quản (cùng một field)

**Thời gian:** 2026-07-22 21:29:58

## Yêu cầu thay đổi

Công ty chủ quản trên màn hình hồ sơ tự cập nhật theo thay đổi ở chi tiết — hai chỗ UI dùng **cùng** field backend `managingCompanyId`.

## Việc đã thực hiện

1. **Seed layout** (`packages/shared`): `managingCompanyId` gắn cả tab `contract` và `personal` (một `storageKey`).
2. **ensureDefaults** (API khởi động) upsert link tab → DB cập nhật khi restart.
3. **EmployeesPage**: reload khi `location.key` đổi (quay lại từ chi tiết) và khi window/tab focus lại.
4. E2e `employee-profile-settings` assert field có trên cả hai tab.

## Kết quả thực hiện

- **Thành công:** form/detail hai tab cùng `managingCompanyId`; list reload sau khi sửa chi tiết.
- Không thêm cột/text riêng — chỉ quan hệ `managingCompany`.
