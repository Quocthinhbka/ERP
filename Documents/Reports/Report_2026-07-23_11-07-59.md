# Report — Thêm nhân viên tổ chức & phạm vi phân quyền

- Thời điểm: 2026-07-23 11:07:59 (local)

## Yêu cầu thay đổi

1. Modal **Thêm nhân viên** trong tổ chức: chỉ nhập **chức vụ**; tài khoản liên kết không bắt buộc; tên lấy từ hồ sơ gắn TK nếu có chọn, ngược lại gán **Chưa có phụ trách**.
2. Phần **phân quyền vị trí**: hiển thị cả đơn vị/công ty hiện tại và các đơn vị cấp dưới thụt lề thể hiện phân cấp (chỉ cấp trên tick chọn được).

## Việc đã thực hiện

**File:** `apps/web/src/pages/setup/OrganizationPage.tsx`

- Thêm hằng `NO_ASSIGNEE_LABEL` và `resolveMemberFields()` — tự gán tên/SĐT/email từ hồ sơ liên kết hoặc fallback `Chưa có phụ trách`.
- Modal **Thêm nhân viên**: bỏ rule bắt buộc TK; khởi tạo `memberName` mặc định khi mở modal.
- Form **Sửa thành viên** và `MembersFormList`: đồng bộ logic tên (bỏ bắt buộc chọn TK).
- `LinkedProfileSelect`: khi bỏ chọn TK → đặt tên về `Chưa có phụ trách`.
- `membersPayloadFromForm()` gọi `resolveMemberFields` trước khi gửi API.
- Thay `getAncestorOptions` bằng `getScopeTreeOptions` + `collectUnitDescendants`:
  - Cấp trên: checkbox chọn phạm vi.
  - Đơn vị/công ty hiện tại: dòng chữ `(đơn vị hiện tại)` / `(công ty hiện tại)`.
  - Đơn vị con: thụt lề theo `depth`, chỉ hiển thị.
- Nhãn form phân quyền: **Phạm vi tổ chức** (thay *Phạm vi cấp trên*).
- Cây tổ chức: tên thành viên rỗng hiển thị qua `displayManagerName`.

## Kết quả thực hiện

- **Thành công** — thay đổi chỉ frontend, không đụng API/backend.
- Typecheck/lint file sửa: không lỗi mới.
- Dev server hot-reload (Vite) — không cần rebuild API.
