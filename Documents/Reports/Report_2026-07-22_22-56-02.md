# Report — Cột Công ty chủ quản trên Quản lý tài khoản

**Thời gian:** 2026-07-22 22:56:02

## Yêu cầu thay đổi

Ở màn Quản lý tài khoản, đổi cột Email thành Công ty chủ quản (phương án A).

## Việc đã thực hiện

1. **API** `apps/api/src/users/users.service.ts`
   - Include `linkedEmployeeProfile.managingCompany.name` khi load user.
   - Response thêm `managingCompanyName` (null nếu chưa liên kết hồ sơ / chưa có công ty).

2. **Web** `apps/web/src/pages/hr/AccountsPage.tsx`
   - Cột bảng: **Công ty chủ quản** (`managingCompanyName`) thay Email.
   - Form tạo/sửa vẫn giữ trường Email.

3. **E2E** `apps/web/e2e/accounts.spec.ts`
   - Assert `columnheader` «Công ty chủ quản»; bỏ assert cell email admin.

## Kết quả thực hiện

| Hạng mục | Kết quả |
|----------|---------|
| List tài khoản | Hiển thị công ty chủ quản từ hồ sơ liên kết |
| Form Email | Giữ nguyên (theo phương án A) |
| Follow-up | Cần rebuild/restart API (`pnpm --filter @erp/api build` + `pnpm dev:down` / `dev:up`) nếu runtime đang dùng `dist` |
