# Report: Đồng bộ dữ liệu công ty chủ quản / tab hợp đồng toàn dự án

**Thời gian:** 2026-07-22 21:19:31

## Yêu cầu thay đổi

Kiểm tra và đảm bảo nhất quán dữ liệu khi thay đổi (công ty chủ quản / tab hợp đồng) — các vị trí tương ứng được cập nhật đồng bộ (phương án A).

## Việc đã thực hiện

### Create / Update API
- `CreateEmployeeDto` bắt buộc `managingCompanyId`; `toCreateScalarData` connect công ty.
- `ensureManagingCompanyAssignable`: tồn tại + status `ACTIVE` (create / check-or-create / update).
- Update từ chối `managingCompanyId` rỗng/null.

### Xóa công ty
- `CompaniesService.remove` chặn nếu còn hồ sơ gắn `managingCompanyId`.

### UI
- Dialog tạo + Select form chỉ hiện công ty `ACTIVE`.

### employee-io v2
- `EMPLOYEE_IO_VERSION = 2`.
- Thêm `employmentStatus` (export/import/diff/apply).
- Resolve công ty chủ quản chỉ trong tập `ACTIVE`.

### Tests
- Helper `ensureTestManagingCompany`.
- Cập nhật e2e create hồ sơ kèm `managingCompanyId` (+ `employmentStatus` nơi cần).
- Unit employee-io + e2e employees / employee-io / profile-settings / permissions: pass.
- Restart `pnpm dev:up`.

## Kết quả thực hiện

- **Thành công:** create/update/IO/list/completion/UI thống nhất quanh công ty chủ quản; IO có đủ field contract chính.
- **Lưu ý:** file Excel/JSON IO cũ (v1) cần tải mẫu mới (version 2).
- **Ngoài phạm vi A:** một số e2e personal/account-employee fail sẵn (403/400 kỳ vọng khác hành vi hiện tại), không thuộc thay đổi này.
