# Report — Trạng thái làm việc + tài khoản liên kết tổ chức

**Thời gian:** 2026-07-22 23:30:57

## Yêu cầu thay đổi

1. Thêm **Trạng thái làm việc** (Đang làm việc / Đang nghỉ / Đang nghỉ phép / Đang vắng mặt / Chưa xác định — mặc định Chưa xác định) vào thông tin hợp đồng.
2. Ở thiết lập tổ chức (người đại diện / phụ trách / thành viên): Select chỉ hiện **tài khoản đã liên kết hồ sơ**; sau khi chọn hiện Họ tên, SĐT, Email, trạng thái làm việc từ hồ sơ.

## Việc đã thực hiện

### Trạng thái làm việc
- Shared: enum `EmployeeWorkPresenceStatus` + `WORK_PRESENCE_STATUS_LABELS`; seed field `workPresenceStatus` tab `contract`.
- Prisma + migration `20260722230000_employee_work_presence_status` (default `UNKNOWN`).
- API employee DTO/service create/update; form/detail UI (`DynamicProfileFields`, `EmployeeFormPage`, …).
- employee-io: snapshot/validate/apply/excel (cột mới cuối sheet, tương thích file cũ thiếu cột → `UNKNOWN`).

### Tổ chức — tài khoản liên kết
- `GET /users?hasLinkedProfile=true` (+ `pageSize` tối đa 200); response thêm `linkedEmployeeProfile` (fullName, phone, email, workPresenceStatus).
- `OrganizationPage`: `LinkedProfileSelect` chỉ list tài khoản có hồ sơ; sau chọn hiện `Descriptions`; autofill tên/SĐT/email denormalized.

## Kết quả thực hiện

| Hạng mục | Kết quả |
|----------|---------|
| Migration | Đã apply |
| Build | shared, employee-io, api, worker OK; employee-io unit tests 7/7 |
| Runtime | `pnpm dev:up` — API health 200 |
| Ghi chú | Giữ nguyên **Trạng thái lao động** (hợp đồng loại NV); field mới là hiện diện làm việc |
