# Report — 2026-07-22 21:50:00

## Yêu cầu thay đổi

Công ty chủ quản trên form sửa hồ sơ không đổi được. Sau kiểm tra: field `managingCompanyId` gắn cả tab Cá nhân và Hợp đồng → hai `Form.Item`/`Select` cùng `name`, Select lỗi hiển thị/search. Phương án **A**: chỉ giữ trên tab Hợp đồng.

## Việc đã thực hiện

1. `packages/shared` — `DEFAULT_PROFILE_FIELDS.managingCompanyId.tabCodes` chỉ còn `contract`.
2. `employee-profile-settings.service.ts` — `ensureDefaults` với field hệ thống: sau khi upsert link theo seed, **xoá** các `EmployeeProfileTabField` không còn trong `tabCodes` (gỡ link personal thừa trên DB hiện có).
3. E2E `employee-profile-settings` — assert field chỉ trên tab contract, không còn trên personal.
4. Build `@erp/shared` + `@erp/api`; restart `pnpm dev:up` với `API_PORT=3001`.

## Kết quả thực hiện

- **Thành công:** layout seed + sync DB chỉ còn một Form.Item công ty chủ quản trên tab Hợp đồng; hết xung đột Select trùng name.
- E2E employee-profile-settings: **2/2 passed**.
- Màn chi tiết vẫn đọc cùng `storageKey`/`managingCompany` khi field có trên tab (chỉ contract sau sync).
- **Ghi chú:** lần GET layout/API start sẽ chạy `ensureDefaults` và gỡ link personal. Cần hard-refresh form edit nếu UI cũ còn cache.
