# Report — Tách module OrganizationPage

## Yêu cầu thay đổi

Tách `OrganizationPage.tsx` (~1865 dòng) thành các module dưới `apps/web/src/pages/setup/organization/`, giữ nguyên hành vi và export `OrganizationPage`; dùng `getApiErrorMessage` từ `../../lib/errors`.

## Việc đã thực hiện

**Tạo mới** (`apps/web/src/pages/setup/organization/`):

- `types.ts` — TreeResponse, UserOption, PermissionGroupOption, ScopeTreeOption, PositionPermissionFormValue, SelectedNode
- `constants.ts` — NO_ASSIGNEE_LABEL, UNIT_MEMBER_KEY_PREFIX, HEADER_HEIGHT, CONTENT_VERTICAL_CHROME, ORG_PAGE_HEIGHT
- `tree-utils.tsx` — nodeKey, unitMemberKey, statusTag, workPresenceTag, positionWorkPresenceTag, findNodeInTree, findUnitMemberInTree, displayText, displayManagerName, personLabel, toTreeData, collectAllKeys, findParentNode, getSiblingInfo, userSelectOptions, workPresenceLabel
- `scope-utils.ts` — scopeTypeLabel, findPathToNode, collectUnitDescendants, getScopeTreeOptions, scopeTreeNodeLabel
- `form-utils.ts` — resolveMemberFields, positionPermissionToForm, positionPermissionToPayload, formatPositionPermissionSummary, memberFieldsToForm, memberToFormValues, membersPayloadFromForm, nodeToFormValues
- `LinkedProfileSelect.tsx`
- `PositionPermissionFields.tsx`
- `MembersFormList.tsx`
- `MembersViewList.tsx` (EmployeesViewList + MembersViewList)
- `index.ts` — re-export public API

**Sửa**:

- `OrganizationPage.tsx` — chỉ còn component chính (~1104 dòng), import từ `./organization`; bỏ local `getApiErrorMessage`, dùng `../../lib/errors`

**Không đổi**: `OrganizationIoActions.tsx`

## Kết quả thực hiện

Thành công. Không lỗi lint trên các file mới/sửa. Typecheck không báo lỗi mới liên quan module tách (ngoài lỗi sẵn có trong repo). Hành vi, `data-testid`, export `OrganizationPage` giữ nguyên.
