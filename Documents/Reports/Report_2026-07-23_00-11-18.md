# Report — Danh sách hồ sơ / tài khoản + nhãn Hình thức lao động

**Thời gian:** 2026-07-23 00:11:18

## Yêu cầu thay đổi

1. Đổi «Trạng thái lao động» → «Hình thức lao động».
2. Quản lý hồ sơ: cột Ảnh, Mã hồ sơ, Họ và tên, SĐT, Công ty chủ quản, Hình thức lao động, Trạng thái hồ sơ, Trạng thái làm việc, Chi tiết (icon mắt).
3. Quản lý tài khoản: Mã tài khoản, Mã vị trí, Hồ sơ liên kết (Họ tên - Mã HS), Ghi chú, Trạng thái, Chi tiết (icon mắt).

## Việc đã thực hiện

- Shared/UI seed: label `employmentStatus` = **Hình thức lao động** (`ensureDefaults` đồng bộ DB).
- API `GET /employees`: thêm `employmentStatus`, `workPresenceStatus`.
- API `GET /users`: `linkedEmployeeProfile.profileCode` (+ fullName).
- `EmployeesPage` / `AccountsPage`: cột theo spec + `EyeOutlined`.
- E2E accounts cập nhật assert cột.

## Kết quả thực hiện

| Hạng mục | Kết quả |
|----------|---------|
| Build / restart | OK, health 200 |
| Follow-up | Duyệt/xác nhận hồ sơ chuyển sang trang chi tiết (không còn trên list). Sửa/xóa tài khoản: list chỉ còn Chi tiết + Thêm mới |
