# Report — TanStack Query (EmployeeForm/ProfileFields), E2E 17/17 pass

- Thời điểm: 2026-07-23 13:44:47 (local)

## Yêu cầu thay đổi

1. Khởi động backend và chạy E2E web (`pnpm --filter @erp/web test:e2e`) với API + DB đang chạy.
2. Migrate TanStack Query cho `EmployeeFormPage`, `EmployeeProfileFieldSettingsPage`.

## Việc đã thực hiện

### TanStack Query (session trước + hoàn tất)

- **`EmployeeFormPage.tsx`**: `useQuery` profile + family/education/work; `useMutation` save/change status; `PageSpinner`; invalidate cache sau mutation.
- **`EmployeeProfileFieldSettingsPage.tsx`**: hook `useEmployeeProfileLayoutMutations.ts`; optimistic `applyLayout` qua `queryClient.setQueryData`.
- **`queryKeys.ts`**: thêm `employeeFamily`, `employeeEducation`, `employeeWork`, `employeeProfileLayout`.

### Hạ tầng dev / proxy

- **`apps/web/vite.config.ts`**: proxy `/api` và `/uploads` theo `process.env.API_PORT` (mặc định 3000).
- **`scripts/dev-start.sh`**: truyền `API_PORT` cho web Vite.
- **`apps/web/playwright.config.ts`**: truyền `API_PORT` cho webServer; project `setup` + `storageState`.

### E2E helpers & auth

- **`e2e/helpers.ts`**: đăng nhập bằng `TEST_ADMIN_ACCOUNT_CODE` (mặc định `Admin`) khi bootstrap trả 403; `getAdminAccessToken`; `expectLoggedInAsAdmin`; xóa cookie trước UI login.
- **`e2e/auth.setup.ts`**: session Playwright dùng chung cho các test đã đăng nhập.
- **`.env.example`**: thêm `TEST_ADMIN_ACCOUNT_CODE=Admin`.

### Cập nhật spec theo UI mới

- Employees: `Quản lý nhân sự`, `Thêm hồ sơ mới`, `Xuất hồ sơ`, tab `Thông tin cá nhân`.
- Organization: nút `Export`/`Import`, `title="Sửa"`, modal `Thêm` exact.
- Profile fields: selector tab button, `data-testid="profile-fields-settings-page"`.
- Login: test account code dùng mã `Admin`; skip email/phone khi admin không có.

## Kết quả thực hiện

- **Backend**: `pnpm dev:up` — API `http://127.0.0.1:3000/api/health`, Web `5173`.
- **E2E**: **17 passed**, **2 skipped** (login email/phone — admin hiện tại không có email/SĐT), **0 failed**.
- **TanStack Query**: EmployeeForm + ProfileFieldSettings đã migrate; build/unit test session trước đã pass.

## Lưu ý / follow-up

- E2E local cần `TEST_ADMIN_PASSWORD` khớp Super Admin trong DB; admin hiện tại đăng nhập bằng mã `Admin`.
- CI (DB trống + bootstrap) vẫn dùng `TEST_ADMIN_EMAIL` — 2 test email/phone sẽ chạy trên CI.
