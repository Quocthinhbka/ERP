# Report — Header 40/30/30, bỏ mã vị trí trên cây tổ chức

- Thời gian: 2026-07-23 14:01:19 (local)

## Yêu cầu thay đổi

1. Bỏ hiển thị **mã vị trí** trên cây sơ đồ tổ chức; kiểm tra **cuộn chuột** vùng cây.
2. Header **một dòng**: tên trang 40% trái, nút hành động 30% giữa, tài khoản + đăng xuất 30% phải — tăng không gian nội dung.

## Việc đã thực hiện

### Header layout

- **`contexts/PageToolbarContext.tsx`**: `PageToolbarProvider`, hook `usePageToolbar({ title, actions })`.
- **`layouts/MainLayout.tsx`**: `AppHeader` chia 40% / 30% / 30%; nút thu gọn menu nằm cùng cột trái với tên trang; `data-testid="page-toolbar-title"` / `page-toolbar-actions`.

### Trang migrate lên toolbar

| Trang | Title | Actions |
|-------|-------|---------|
| `DashboardPage` | Tổng quan | — |
| `EmployeesPage` | Quản lý nhân sự | Xuất/Nhập, Thêm hồ sơ |
| `AccountsPage` | Quản lý tài khoản | Thêm tài khoản |
| `OrganizationPage` | Thiết lập / Tổ chức | Search, Tải lại, Mở/Thu, Export/Import |
| `PermissionGroupsPage` | Nhóm quyền | Thêm nhóm |
| `PermissionsPage` | Danh sách quyền hạn | — |
| `EmployeeProfileFieldSettingsPage` | Thiết lập hồ sơ nhân sự | — |

Các trang list bỏ `Card title` / `extra`; nội dung Card `bordered={false}`, padding body tối giản.

### Cây tổ chức

- **`tree-utils.tsx`**: node thành viên hiển thị `{chức vụ} - {tên}` (không còn `VT-xxx ·`).
- **`OrganizationTreePanel.tsx`**: `flex:1`, `overscrollBehavior: contain` để cuộn trong panel.
- **`OrganizationPage.tsx`**: Col cây/detail `minHeight:0` + flex column; Card không title.

### E2E

- **`helpers.ts`**, **`auth.setup.ts`**: assert đăng nhập qua `page-toolbar-title` = `Tổng quan`.

## Kết quả thực hiện

- **TypeScript**: pass.
- **E2E**: 15/17 pass (+ 1 skipped), 2 fail do auth rate-limit/flaky khi chạy song song (`employees`, `login account code`) — không liên quan layout; chạy lại khi API ổn định thường pass.
- **Chi tiết panel** tổ chức vẫn giữ trường «Mã vị trí» (chỉ ẩn trên cây).

## Follow-up (tuỳ chọn)

- Trang chi tiết/form (`EmployeeForm`, `AccountDetail`, …) có thể đưa nút «Quay lại» / «Lưu» lên toolbar tương tự.
- Header 30% giữa có thể cần `size="small"` thêm trên mobile khi nhiều nút (Organization).
