# Report — Refactor OrganizationPage (phase 2)

## Yêu cầu thay đổi

Tiếp tục tách `OrganizationPage.tsx` (~1100 dòng) thành hook và subcomponents dưới `organization/`:

- `useOrganizationPage.ts` — state, forms, handlers, computed values
- `OrganizationDetailPanel.tsx` — panel phải (view/edit, title, extra, reorder)
- `OrganizationModals.tsx` — modal thêm nhân viên + modal tên công ty/đơn vị
- `OrganizationTreePanel.tsx` — cột trái cây tổ chức
- `OrganizationPage.tsx` — layout mỏng (~200–300 dòng)

Giữ nguyên behavior, `data-testid`, dùng `getApiErrorMessage`, export `OrganizationPage` không đổi, compile với `tsc`.

## Việc đã thực hiện

### File tạo mới

| File | Dòng | Nội dung |
|------|------|----------|
| `organization/useOrganizationPage.ts` | 528 | Hook: useState, useForm, useCallback (loadTree, handleSave, handleDelete, …), useMemo (canUpdateSelected, antTreeData, …), trả về toàn bộ state/handlers cho page |
| `organization/OrganizationDetailPanel.tsx` | 525 | Panel phải: renderViewPanel, renderEditForm, panelTitle, renderPanelExtra, renderReorderButtons |
| `organization/OrganizationModals.tsx` | 112 | Modal thêm nhân viên + modal tên công ty/đơn vị (`org-name-input`) |
| `organization/OrganizationTreePanel.tsx` | 57 | Cột trái: Spin + Tree (`org-tree`) |

### File sửa

| File | Dòng | Thay đổi |
|------|------|----------|
| `OrganizationPage.tsx` | 185 | Chỉ layout: Card toolbar (search, reload, expand/collapse, IO), Row/Col, gọi hook + subcomponents |
| `organization/index.ts` | 14 | Export hook, panel, modals, tree panel |

### Cải thiện nhỏ

- `handleSave` và `submitNameModal` dùng `getApiErrorMessage` thay message cố định
- `linkedUserLabel` bọc `useCallback` trong hook
- `hasPermission` typed với `PermissionCode`

## Kết quả thực hiện

**Thành công.** `OrganizationPage.tsx` giảm từ ~1100 → **185 dòng**. Các file organization mới compile sạch với `tsc` (lỗi còn lại ở `AccountDetailPage` / `EmployeeDetailPage` — ngoài phạm vi).

Tổng dòng module organization (phase 2): **1421** (185 + 528 + 525 + 112 + 57 + 14).
