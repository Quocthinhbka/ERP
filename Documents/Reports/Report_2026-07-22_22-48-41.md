# Report — Redis local + cleanup dữ liệu test

**Thời gian:** 2026-07-22 22:48:41

## Yêu cầu thay đổi

Đồng bộ Redis để import/export (BullMQ) chạy đúng trên môi trường local, và xóa dữ liệu test còn sót sau các lần e2e.

## Việc đã thực hiện

1. **`scripts/dev-start.sh`**
   - Đọc `REDIS_HOST` / `REDIS_PORT` / `REDIS_PASSWORD` từ file `.env` (không phụ thuộc biến shell Agent cũ).
   - Truyền tường minh các biến Redis khi start API và Worker (ghi đè shell, ví dụ `REDIS_PORT=6380` lỗi thời).
   - Log cổng Redis khi `pnpm dev:up`.

2. **Restart runtime**
   - `pnpm dev:down` → `pnpm dev:up`.
   - Worker: `localhost:6379`, queues `erp-organization-io` / `erp-employee-io`.
   - API health `:3001` → 200.

3. **Cleanup DB**
   - Hồ sơ test (`blankgender*`, import e2e…): không còn sót (đã được afterAll / lần chạy lại dọn).
   - Xóa 3 công ty test: `E2E Co 1784731738573`, `E2E Co 1784731789096`, `E2E Co 1784733338707`.

## Kết quả thực hiện

| Hạng mục | Kết quả |
|----------|---------|
| Redis Worker | Thành công — kết nối **6379** theo `.env` |
| API / Web | Alive, health 200 |
| Cleanup | 3 company E2E đã xóa; không còn company tên chứa `E2E` |
| Hạn chế | Redis local không yêu cầu password → ioredis cảnh báo `password was supplied` (không chặn IO). Chuẩn repo/docker vẫn là **6380**; máy hiện không có Docker nên giữ **6379**. |
