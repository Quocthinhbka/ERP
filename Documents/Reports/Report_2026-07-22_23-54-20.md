# Report — Mã vị trí VT + cột Quản lý tài khoản

**Thời gian:** 2026-07-22 23:54:20

## Yêu cầu thay đổi

1. Sinh mã vị trí cho mọi vị trí có thể liên kết tài khoản.
2. Thay cột **Công ty chủ quản** bằng **Mã vị trí** trên Quản lý tài khoản.
3. Cho phép chức vụ / phân quyền trên node không phải lá (mở rộng holder).

## Việc đã thực hiện

### Schema / migration
- Cột `position_code` (unique) trên: Organization, Company, OrganizationUnit, OrganizationMember, CompanyMember, OrganizationUnitMember.
- Sequence `org_position_code_seq` → mã `VT-00001`…
- Enum thêm `ORGANIZATION_MEMBER`, `COMPANY_MEMBER`.
- Migration `20260722234500_org_position_codes` + backfill dữ liệu hiện có.

### API
- `allocatePositionCode` khi tạo seat (org/company/unit/member).
- Sync member upsert giữ mã cũ; org/company member có `positionPermission`.
- Auth: tìm holder gồm org/company members; bỏ chặn leaf-only cho unit member.
- Tree trả `positionCode` + PP member trên mọi cấp.
- `GET /users`: `positionCodes[]` (thay `managingCompanyName` trên list).

### Web
- Accounts: cột **Mã vị trí**.
- Organization: thành viên + phân quyền trên mọi node; hiện mã VT trên cây/chi tiết.
- E2E accounts assert cột Mã vị trí.

## Kết quả thực hiện

| Hạng mục | Kết quả |
|----------|---------|
| Migration / build | OK |
| Runtime | `dev:up`, health 200 |
| Chuỗi nghiệp vụ | Hồ sơ ↔ Tài khoản ↔ Vị trí (VT) ↔ Quyền nhóm gắn vị trí |
| Follow-up | organization-io có thể cần map `positionCode` nếu export/import org |
