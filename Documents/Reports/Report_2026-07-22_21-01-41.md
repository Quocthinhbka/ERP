# Report: Công ty chủ quản trên tab hợp đồng + IO

**Thời gian:** 2026-07-22 21:01:41

## Yêu cầu thay đổi

- Thêm **Công ty chủ quản** vào tab **Thông tin hợp đồng** (bắt buộc).
- Cập nhật logic import/export hồ sơ nhân viên theo field này.

## Việc đã thực hiện

### Shared / profile settings
- Seed field system `managingCompanyId` (required, locked) trên tab `contract`.
- Thêm vào `BUILTIN_PROFILE_STORAGE_KEYS`.
- `ensureDefaults` đồng bộ `required` cho field `locked`.

### API
- `UpdateEmployeeDto.managingCompanyId` + validate công ty tồn tại khi cập nhật.

### Web
- Form động: Select tải `/organization/companies`.
- Chi tiết: hiển thị tên công ty.
- Meta field trong `employee-fields.tsx`.

### employee-io
- Cột/snapshot `managingCompanyName` (bắt buộc).
- Export lấy tên từ quan hệ; import resolve theo tên (không phân biệt hoa thường, báo lỗi nếu thiếu/trùng).
- Excel header + guide + validate + apply/create/update.

### Test
- Unit employee-io: pass.
- E2E `employee-io` + `employee-profile-settings`: pass.
- Restart `pnpm dev:up` sau build.

## Kết quả thực hiện

- **Thành công:** tab hợp đồng có công ty chủ quản bắt buộc; IO Excel/JSON có `managingCompanyName`.
- **Lưu ý:** file mẫu/export cũ thiếu cột sẽ bị validate từ chối — cần tải mẫu mới.
- **Follow-up:** nếu nhiều công ty trùng tên, import sẽ lỗi rõ ràng (cần đặt tên unique hoặc mở rộng match theo mã sau).
