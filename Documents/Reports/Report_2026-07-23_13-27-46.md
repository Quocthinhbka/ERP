# Report — Tiếp tục cải thiện Frontend (phase 2)

**Thời gian:** 2026-07-23 13:27:46

## Yêu cầu thay đổi

Tiếp tục các hạng mục follow-up sau phase 1: mở rộng TanStack Query, tách tiếp OrganizationPage, chuẩn hóa query keys.

## Việc đã thực hiện

### TanStack Query mở rộng
- **`src/lib/queryKeys.ts`**: Query keys tập trung cho accounts, employees, permissions, organization, profile layout.
- **`AccountsPage`**: `useQuery` + `useMutation` tạo tài khoản; invalidate cache sau mutation.
- **`AccountDetailPage`**: `useQuery` parallel fetch; `PageSpinner`; giữ UX quyền hiệu lực.
- **`PermissionGroupsPage`**: Dual query (groups + catalog) + mutations create/update/delete.
- **`EmployeeDetailPage`**: `useQuery` fetch profile; `invalidateQueries` sau edit-request/review.
- **`PermissionsPage`**, **`EmployeesPage`**, **`useEmployeeProfileLayout`**: Dùng `queryKeys` thay chuỗi literal.

### Tách OrganizationPage (phase 2)
| File | Dòng | Vai trò |
|------|------|---------|
| `OrganizationPage.tsx` | ~185 | Layout + toolbar |
| `useOrganizationPage.ts` | ~528 | State, handlers, computed |
| `OrganizationDetailPanel.tsx` | ~525 | Panel phải view/edit |
| `OrganizationModals.tsx` | ~112 | Modal nhân viên + tên |
| `OrganizationTreePanel.tsx` | ~57 | Cây tổ chức |

### Testability
- `data-testid`: `accounts-page`, `account-create-btn`, `permission-groups-page`

## Kết quả

- `pnpm test:unit` — 8/8 pass
- `pnpm build` — pass
- OrganizationPage: **1100 → 185 dòng** (page chính)

## Follow-up

- Chạy e2e: `pnpm --filter @erp/web test:e2e` (cần API + DB)
- TanStack Query cho `EmployeeFormPage`, `EmployeeProfileFieldSettingsPage`
- Giảm chunk `errors-uVdb5DQ5.js` (~541 kB) — tách axios/antd nếu cần
